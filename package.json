{
  "name": "d-math",
  "version": "1.0.1",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib --source-maps inline --copy-files",
    "release:patch": "npm version patch -m 'upgraded to %s' && git push && git push --tags && npm publish",
    "release:minor": "npm version minor -m 'upgraded to %s' && git push && git push --tags && npm publish",
    "release:major": "npm version major -m 'upgraded to %s' && git push && git push --tags && npm publish"
  },
  "keywords": [],
  "author": "dharmendra <dharmendras@zeta.tech> (zeta.in)",
  "license": "UNLICENSED",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "dependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/Dharmendra205111014/d-math"
  }
}
